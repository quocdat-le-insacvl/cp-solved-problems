#pragma GCC optimize("O3,unroll-loops")

#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL
#include "/home/nightfury/mycp/debug.h"
#else
#define dbg(...) 42
#endif

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
	int n, m, r, c, x, y;
	cin >> n >> m >> r >> c >> x >> y;
	vector<string> a(n);
	for( int i = 0 ; i < n; i++) {
		cin >> a[i];
	}
  return 0;
}
